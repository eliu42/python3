4.7.5. Unpacking Argument Lists

The reverse situatoin occurs when the arguments are already in a list or tulpe but need to be unpacked for a function call requiring separate positional arguments. For instance, the built-in range() function expects separate start and stop arguments. If they are not availale separate, write the function call with the *-operator to unpack the arguments out of a list or tuple:

See 4.7.5.py
